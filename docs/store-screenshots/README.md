# Play Store screenshots (draft)

Captured 2026-07-17 from a real Android debug build (emulator, `1080x2400`) for
Intent 0.5 (`docs/UPGRADE-PLAN.md`). Not final - review before uploading.

- `01-celebration.png` - the celebration modal after completing Sandhyavandhanam,
  showing the new motion/haptic polish and saffron share-card.
- `02-today.png` - Today page with the day fully complete (streak 1), panchangam
  info box visible at the top.
- `03-learning.png` - Learning page, Hanuman Chalisa verse-by-verse with the
  English/Malayalam/Sanskrit language toggle.
- `04-profile.png` - Profile page: tier progress, streak/best/punya stats,
  family members, referral card.
- `05-sabha.png` - Sabha leaderboard. Taken from a disposable single-user test
  account (`e2efull@nithyakarma.test`), so it only shows one row - **recapture
  this one from a real account with other members opted in** before using it in
  the listing, since an empty-looking leaderboard undersells the feature.

All captured on the `HapticsTest` throwaway profile (test account, deleted and
reseeded after capture per project convention - see memory `e2efull-account`).
Display name and streak numbers are test data, not representative - swap for a
cleaner run (e.g. a longer streak, more realistic display name) if these ship
as-is rather than getting recaptured closer to launch.
