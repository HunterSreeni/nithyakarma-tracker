node_modules/
dist/
dev-dist/
test-results/
playwright-report/
.playwright-cli/
*.log
.DS_Store
*.apk
.env
google-services.json
fcm-service-account.json
*firebase-adminsdk*.json
client_secret_*.json

# Android release signing - keystore + its passwords. Never commit (see
# app/android/keystore.properties.example for the template).
keystore.properties
*.jks
*.keystore

# supabase CLI temp (project uses Supabase MCP only)
app/supabase/.temp/

# duplicate/superseded migration reconstructions (content already committed
# upstream under different timestamps) - kept locally for reference only
app/supabase/migrations/_archive/

# Local Netlify folder
.netlify

# Physical Pambu Panchangam photos/PDF - spot-check oracle only, per its own
# "no copying" notice (see docs/architecture/08-PANCHANGAM.md). Never commit.
paambu-panchangam/
parapava-varusham-pambu-panchangam-2026-2027_compress.pdf

# Malayalam (Kerala Brahmana Sabha) panchangam photos - copyrighted, spot-check
# oracle only. Never commit.
malayalam-panchangam/

# Stray secret file - never commit
google-secret.txt
